#!/bin/sh
#
# script/build: builds the project
#

set -e

cd "$(dirname "$0")/.."

script/update

# Build the image
docker build -t mysocietyorg/foi_monitor:latest .

# Tag it with a timestamp
NOW=$(date +%s)
docker tag mysocietyorg/foi_monitor:latest mysocietyorg/foi_monitor:${NOW}
